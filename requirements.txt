# Baseline environment (Python 3.11)
# PyTorch is resolved transitively by LeRobot. Pin the CUDA build separately
# on Kaggle after checking the runtime's NVIDIA driver.
lerobot[smolvla]==0.4.4

# Reproducible experiment configuration.
hydra-core==1.3.5
wandb==0.24.2

# Reading and converting canonical LIBERO HDF5 demonstrations.
h5py==3.16.0

# Official LIBERO environment runtime for eval.py.
robosuite==1.4.0
bddl==1.0.1
gym==0.25.2
mujoco==2.3.7
future==0.18.2
easydict==1.13
matplotlib==3.11.1
